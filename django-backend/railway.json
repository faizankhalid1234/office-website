{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python setup.py && gunicorn config.wsgi:application --bind 0.0.0.0:$PORT",
    "healthcheckPath": "/admin/login/",
    "restartPolicyType": "ON_FAILURE"
  }
}
