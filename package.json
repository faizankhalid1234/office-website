{
  "name": "office-expense",
  "private": true,
  "scripts": {
    "dev": "concurrently -n backend,website,admin -c blue,green,magenta \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\" \"npm run dev --prefix admin\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:website": "npm run dev --prefix frontend",
    "dev:admin": "npm run dev --prefix admin",
    "build": "npm run build --prefix backend && npm run build --prefix frontend && npm run build --prefix admin",
    "install:all": "npm install --prefix backend && npm install --prefix frontend && npm install --prefix admin",
    "setup": "npm run install:all && npm run db:seed",
    "db:seed": "npm run db:seed --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
